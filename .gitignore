*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Local dev toolchain (portable PHP + Composer) — not for deployment
/.dev-tools

# Local SQLite dev database (seeded demo data — production uses MySQL)
/database/*.sqlite
/database/*.sqlite-journal

# Real server env (only the .example template is committed)
/.env.hostinger
